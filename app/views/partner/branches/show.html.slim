p#notice= notice

.widget.fluid
  .whead
    h6= t :edit_branch
    .clear

  table

/= admin_content_form_for @branch, :url => (@branch.new_record?? partner_branches_path : partner_branch_path(@branch)), :wrapper => :admin_content, :defaults => {:input_html => {:readonly => true}} do |f|
/  fieldset
/    .widget.fluid
/      .whead
/        h6= t :edit_branch
/        .clear
/      = f.input :name
/      /= f.input :address.phone, :as => :string
/      /= f.input :addrsss.fax
/      = f.input :email, :as => :string
/      = f.input :office_hours
/      .formRow.noBorderB
/        #status1.status
/        .formSubmit
/          = link_to 'Back', partner_branches_path, :class => "buttonM bDefault ui-wizard-content ui-formwizard-button"
/          .clear
/        .clear
